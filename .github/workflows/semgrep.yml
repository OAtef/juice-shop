name: Semgrep Security Scan

on:
  push:
    branches: ["main", "master"]
  pull_request:

jobs:
  sast_scan_semgrep:
    runs-on: ubuntu-latest
    # This allows the pipeline to continue even if vulnerabilities are found, 
    # matching the "allow_failure: true" and "|| true" from your snippet.
    continue-on-error: true 

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Semgrep and Generate JSON
        # We use the semgrep container to run the scan
        run: |
          docker run --rm -v $(pwd):/src returntocorp/semgrep semgrep ci --json --output=semgrep.json --config auto || true

      - name: Beautify JSON output
        # GitHub's ubuntu-latest runner has 'jq' pre-installed
        run: |
          cat semgrep.json | jq . > semgrep_beautified.json

      - name: Upload Beautified Report
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: semgrep_beautified.json

      # ADD THIS STEP:
      - name: Save Scan Results
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-json
          path: semgrep.json

  upload_reports:
    runs-on: ubuntu-latest
    needs: sast_scan_semgrep  # Ensures this runs AFTER the scan
    if: always()             # Runs even if the scan found bugs
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Retrieve Scan Results
        uses: actions/download-artifact@v4
        with:
          name: semgrep-json

      - name: Setup Python
        uses: actions/setup-python@v5

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          pip install requests

      - name: Upload to DefectDojo
        env:
          DOJO_HOST: "demo.defectdojo.org"
          DOJO_USER: ${{ secrets.DOJO_USERNAME }}
          DOJO_PASS: ${{ secrets.DOJO_PASSWORD }}
        run: |
          # 1. Fetch the API Token dynamically
          API_KEY=$(curl -s -X POST -H 'Content-Type: application/json' \
            "https://$DOJO_HOST/api/v2/api-token-auth/" \
            -d "{\"username\": \"$DOJO_USER\", \"password\": \"$DOJO_PASS\"}" | jq -r '.token')

          # 2. Run the instructor's Python script
          # Note: Ensure 'upload-results.py' is in your repo root
          python3 upload-results.py \
            --host "$DOJO_HOST" \
            --api_key "$API_KEY" \
            --engagement_id 24 \
            --product_id 5 \
            --lead_id 1 \
            --environment "Production" \
            --result_file semgrep.json \
            --scanner "Semgrep JSON Report"

  # sca_scan_trivy:
  #   runs-on: ubuntu-latest
  #   continue-on-error: true # Matches allow_failure: true
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     # Log in to GitHub Container Registry (or Docker Hub)
  #     - name: Login to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     # Build the image locally (Since Juice Shop needs an image to scan)
  #     - name: Build image
  #       run: docker build -t juice-shop:latest .

  #     # Run Trivy to generate the SBOM and Scan
  #     - name: Run Trivy SCA
  #       uses: aquasecurity/trivy-action@0.20.0
  #       with:
  #         image-ref: 'juice-shop:latest'
  #         format: 'json'
  #         output: 'trivy-sca.json'
  #         scan-type: 'image'

  #     # Upload the result so the 'upload_reports' job can use it
  #     - name: Save Trivy Results
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: trivy-json
  #         path: trivy-sca.json

  sca_scan_trivy:
    runs-on: ubuntu-latest
    continue-on-error: true # Equivalent to allow_failure: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Using the official action is cleaner than wget/tar
      - name: Run Trivy FS Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          format: 'json'
          output: 'trivy-sca.json'
          # This matches your instructor's specific logic for filesystem scanning

      - name: Save Trivy Results
        uses: actions/upload-artifact@v4
        with:
          name: trivy-json
          path: trivy-sca.json